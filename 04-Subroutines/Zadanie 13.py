tablica = [4,3,7,1,3]

def suma(tablica):
    suma = 0
    print(f"Tablica: {tablica}")
    for i in range(len(tablica)):
        suma+=tablica[i]
    print(f"Suma wartości: {suma}")
suma(tablica)
        

        